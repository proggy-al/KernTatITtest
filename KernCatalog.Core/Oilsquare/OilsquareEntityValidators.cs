// Сгенерировано при помощи утилиты Tn.Sdk.Cli.

namespace KernCatalog.Core;

/// <summary>Проверка корректности: <see cref="OilsquareEntity"/>.</summary>
/// <remarks>Db: kern_catalog.oilsquare.</remarks>
public partial class OilsquareEntityValidators 
{

	 static OilsquareEntityValidators()
	{
		// Тут можно влиять на валидатор сущности.
		//OnValidatorBuild += (validator, localizer) =>
		//{
			//validator.RuleFor(o => o.PropertyName).ValidCondition();
		//};
	}
}
